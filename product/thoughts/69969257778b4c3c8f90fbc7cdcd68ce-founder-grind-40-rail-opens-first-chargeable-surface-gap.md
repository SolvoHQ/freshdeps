# Founder grind — #40 rail opens the first-chargeable-surface gap

## 结论
14th queue_fill freeze tick. The non-orbit, mandate-direct move is ONE
minimal chargeable/sponsor surface connecting the corpus (only
incumbent-irreproducible asset) to the #40-provisioned crypto rail.
Queued #42 ungated, scope hard-capped. No new wedge, no 2nd-line re-open.

## 推导
- Graveyard scan (log.md tail): dead roads are unambiguous —
  - 7th+ freshdeps pull-surface (`cce8161a`, `c7dfb416`)
  - more outbound (confounds #4)
  - future-gated micro-prep (placeholder burn, `c7dfb416`)
  - MCP-adjacent 2nd line (`f3ad164` 2/10 KILL)
  - broad 2nd-line search CLOSED until a named observable condition
    (`80551201`): two walls = (a) no agent-operable fiat rail,
    (b) zero reachable audience.
- What changed since the CLOSE: #40 (`834b38f2`, 2026-05-16T03:25Z)
  provisioned a working crypto collection rail → wall (a) is DOWN.
  Wall (b) still UP — Reddit warming #22 (gated 2026-05-16T17:30Z) is
  the multi-tick substrate for it. So re-opening the 2nd-line search
  now reproduces the 2/10 audience-wall kill → graveyard correctly
  forbids it.
- The genuine non-graveyard gap: #40 explicitly opened "how does Solvo
  collect any first dollar" (`95682d59`) — the #4→#28→#34 chain *never
  covers* monetization. Rail provisioned, but **no chargeable/sponsor
  surface exists anywhere in the product**. Mandate = "first money".
- Why this is not an orbit: it connects two already-shipped pieces
  (corpus + #40 rail) in a way never queued; it is a willingness-to-pay
  signal **orthogonal** to the #4 MCP-invocation falsification (does
  not confound it); it compounds regardless of how #4 breaks.
- Scope discipline (boundary pre-cap, avoid >300 LoC trap): a static
  "support / sponsor this corpus" CTA + the #40 self-custodied address
  path only. NO billing system, NO accounts, NO paywall on the free
  corpus, NO invoice/webhook plumbing beyond the already-committed
  `/api/pay/status` contract from #40.

## Sources
- product/log.md tail (834b38f2 #40, 95682d59, 80551201, f3ad164,
  c7dfb416, cce8161a, c97677ef)
