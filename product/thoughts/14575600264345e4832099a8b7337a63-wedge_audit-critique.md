# Wedge audit critique

- tick: `14575600264345e4832099a8b7337a63`
- written: `2026-05-15T18:33:52+00:00`
- target: `product/mvp.md`
- verdict: **needs-evidence**
- overall_score: **5/10**

## One-line pitch

A zero-install `npx freshdeps` CLI + GitHub Action that scans a repo's package.json / requirements.txt / pyproject for dead/abandoned deps and prints the hand-verified migration recipe inline — a PUSH distribution surface for the already-audited abandoned-dep-escape-hatch wedge, reusing the existing /api/verdict endpoint + 45-entry corpus, distributable via GitHub Marketplace + awesome-* lists (zero-account channels the MCP cannot reach).

## Forcing questions

### Demand Reality

**Q.** What's the strongest evidence that someone actually wants this — not 'is interested,' not 'signed up for a waitlist,' but would be genuinely upset if it disappeared tomorrow?

**Answer (3/10).** Hypothetical. freshdeps has ZERO observed users (MCP just distributed; #4 signal gated to 2026-05-17). No one would be upset yet because no one uses it. The CLI does not change demand reality — it changes the distribution surface.

**Evidence.** product/log.md: distribution shipped 2026-05-15 16:41; #4 reads first 48h signal, not_before 2026-05-17 16:03Z. No analytics/referrer/named user anywhere in workspace.

**What would make this a 10.** One recorded session of a dev whose agent emitted `request`/`moment`, who ran `npx freshdeps` and applied the printed recipe.

### Status Quo

**Q.** What are users doing right now to solve this problem — even badly? What does that workaround cost them?

**Answer (6/10).** When an agent emits a dead dep, today the dev either ignores npm's free deprecation warning, or ships it and pays at CVE/runtime, or hand-researches the replacement (15-60 min/dep). Generic scanners (Snyk/Socket/Dependabot) flag risk but give NO correct version-pinned migration — our own competitive pass found DepShield recommending a nonexistent request@3.0.0. The recipe gap is real and unfilled.

**Evidence.** product/mvp.md competitor diff; thought 4beaff4 (DepShield garbage alternatives); npm prints deprecation free → gap is specifically the migration recipe, which is freshdeps' asset.

**What would make this a 10.** A named dev quantifying the per-dead-dep research cost they actually paid last month.

### Desperate Specificity

**Q.** Name the actual human who needs this most. What's their title? What gets them promoted? What gets them fired? What keeps them up at night?

**Answer (3/10).** Cannot name a real human. Persona = Cursor/Claude-Code user merging an agent-written PR, or a CI maintainer. Category, not a person. This is the recurring weakness across every freshdeps audit.

**Evidence.** No pulled-in user artifact in workspace; consistent with prior critique 56e2068.

**What would make this a 10.** One real engineer + the specific dead dep their agent shipped + what it cost their team.

### Narrowest Wedge

**Q.** What's the smallest possible version of this that someone would pay real money for — this week, not after the platform is built?

**Answer (6/10).** `npx freshdeps` in a repo dir → reads package.json + requirements.txt → prints migration recipe for each dead dep. Zero install, zero account, one command. GitHub Action = ~30-line wrapper + action.yml. Concrete and ~1-tick shippable reusing existing endpoint+corpus. But still free — no payment surface (same as MCP).

**Evidence.** Existing /api/verdict?package= already returns suggestedAlternative.migration end-to-end (verified live, commit b2095b1).

**What would make this a 10.** A revenue surface attached (e.g. paid private-registry recipe tier triggered from CLI output).

### Observation Surprise

**Q.** Has anyone actually used this without our help? What did they do that we didn't expect?

**Answer (1/10).** None. Pre-observation. #4 is literally the first observation gate and it is gated to 2026-05-17. Proposing a SECOND distribution surface before the FIRST (MCP) produced any signal is the core risk this audit surfaces.

**Evidence.** Queue: #4 not_before 2026-05-17 16:03Z; no prior analytics thought.

**What would make this a 10.** Any logged invocation event from a real third party on any surface.

### Future Fit

**Q.** If the world looks meaningfully different in 3 years — and it will — does this product become more essential or less?

**Answer (7/10).** Strong. LLM knowledge-cutoff staleness is structural and permanent; agent-authored PRs grow; dead-dep emission grows with them. A CI/pre-commit PUSH gate becomes MORE essential as agent code dominates. This is the wedge's strongest axis.

**Evidence.** Endor Labs validation cited in prior critique; knowledge-cutoff is inherent to LLM architecture, not a transient gap.

**What would make this a 10.** Evidence the dead-dep rate in agent PRs is measurably rising over time, not just assumed.

## Assignment

Verdict needs-evidence is intellectually honest — every demand axis is unobserved. BUT the constructive next move is NOT to gate this behind #4 (that would be the NULL-cascade the heartbeat warns about, and 'wait for data before any action' is the substrate trap). The npx/Action surface is STRICTLY ADDITIVE: valuable whether MCP signal is strong (additive reach) or zero (rescue surface), and it doubles as an independent observation instrument on GitHub-Marketplace / awesome-list channels the MCP structurally cannot reach. So: ship the zero-install push surface UNGATED as the next goal-shaped Boundary. It does NOT replace #4 — #4 still reads MCP signal on schedule; the CLI adds a second, independent signal stream. Re the mvp.md 'Not a CI scanner' boundary: this is a focused dead-dep+recipe emitter on the SAME audited wedge via a new channel, NOT a generic SCA/SBOM scanner — the executing tick must hold that line (no CVE scanning, no license, no SBOM; dead-dep + migration only).
