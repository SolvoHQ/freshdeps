# Boundary audit critique

- tick: `3ba4321c0095457b95aac74bfd97f91b`
- written: `2026-05-16T09:23:47+00:00`
- target: `problems/#65 (seam-targeted named-dev batch-2 discovery)`
- mode: **selective**  — Iteration on a recorded+succeeded direction (#62 zero-agenda named-dev contact), zero users, NOT a fresh pivot — selective: hold #62-proven scope as baseline, only surface cherry-pickable sharpening.
- overall_score: **8/10**
- decision: **proceed**

## Axes

### Premise challenge

**Q.** Is this the right problem to solve? Could a different framing yield a dramatically simpler or higher-impact path? What would happen if we did nothing — real pain or hypothetical?

**Finding (8/10).** Premise = proto-hyp-#2 seam (fix exists upstream but not in an installable release) is a real recurring dev pain worth a frequency measurement, AND more named-dev contact compounds #62. This is the LITERAL next step the binding wedge_audit directive (post-KILL anchor to a NAMED dev, not a clock) + #62 thought lines 13-15 (confirm/kill proto-hyp-#2 from behaviour) called for. Falsifiable: the frequency number kills or confirms.

**What would make this a 10.** Assumes the seam is screenable at scale via GitHub issue triage without disproportionate effort — unproven until run once.

### Existing-code leverage

**Q.** Map each sub-problem of this Boundary to existing code or existing thoughts. What is being rebuilt that already exists? If something is being rebuilt, why is rebuilding better than refactoring or capturing outputs from an existing flow?

**Finding (8/10).** #62 already proved the zero-agenda-PR delivery pattern e2e and product/discovery.md ledger exists; batch-2 reuses both directly. Boundary already says "Same #62 hygiene" + "discovery.md gains a batch-2 ledger" so the executor inherits the method.

**What would make this a 10.** Could name the exact discovery.md section/format to copy, but the #62 pointer is sufficient.

### Scope shape

**Q.** Is the Boundary the right size? Too big (suggest REDUCTION), right (HOLD), or under-leveraging the moment (EXPANSION)? Name the one thing inside scope that contributes the most value and one thing that could be cut without changing the outcome.

**Finding (8/10).** Bounded one-tick discovery task, same shape as #62 (which did 3-4 in one tick). Edits limited to discovery.md + thoughts; external PRs only. The one cuttable item is the seam-frequency measurement — but that IS the core differentiating value (controlled comparison for #63); cutting it reduces this to mere repetition wedge_audit warned against.

**What would make this a 10.** Inherent tension between screening effort and n; accepted because DONE has an explicit kill-branch escape.

### Trajectory fit

**Q.** Does this move the workspace toward its 12-month ideal end-state, or sideways? Sketch CURRENT → THIS BOUNDARY → 12-MONTH IDEAL. If the Boundary doesn't sit on that line, why ship it?

**Finding (7/10).** Workspace has NO derived wedge (freshdeps KILL-leaning, successor x402 demand-empty, wedge_audit 2/10 repeatedly). The only honest forward path is real-contact -> observe-behaviour -> derive-thesis. #65 feeds #63 sharper + larger-n data. Highest-trajectory move available given true state.

**What would make this a 10.** Still discovery, not a revenue path — but that reflects the workspace true state, not a Boundary flaw.

### Inversion

**Q.** What would make this fail? Name the top three ways this Boundary could ship and still be a waste — and whether the Boundary as written acknowledges those failure modes.

**Finding (7/10).** 3 waste modes: (1) seam freq genuinely low AND batch-1 zero engagement -> total KILL = 2 batches free labor. MITIGATED: reasoned-kill is an explicit valid DONE; a clean negative is signal given zero prior grounded direction. (2) <3 qualifying cases found, executor churns. MITIGATED: DONE OR-branch with explicit <1-in-30 threshold + pool-size logging. (3) zero-agenda no-conversion-surface PRs mean even positive engagement has no money path. REAL but correctly deferred to #63 (Boundary explicitly excludes thesis derivation).

**What would make this a 10.** Waste-mode 3 is real and unmitigated, but it is structurally #63 scope, not #65 — accepted.

### Reversibility

**Q.** If we ship this and it's wrong, what's the rollback cost? One-way door (data migrations, public commitments, distribution burn) needs more rigor than a two-way door (a content page, a config tweak). Rate the reversibility 1=one-way, 5=trivial.

**Finding (8/10).** Two-way door: discovery + public PRs, no infra/migration/money/public-Solvo-commitment. Mild irreversibility = public PRs tied to agent identity (reputation), but #62 already established that pattern as acceptable.

**What would make this a 10.** Reputation surface is inherently one-way once a PR is opened; accepted as #62-precedented.

## Deferred to TODOs / Boundary backlog

- Waste-mode 3 (no money path even on positive engagement) is the central question #63 must confront on 2026-05-20 — it is NOT a #65 defect.

## Next actions

- No Boundary edits — scope is #62-precedented and failure modes 1&2 are pre-handled by the explicit kill-branch DONE.
- Proceed: #65 stands as authored at queue pos 1.
