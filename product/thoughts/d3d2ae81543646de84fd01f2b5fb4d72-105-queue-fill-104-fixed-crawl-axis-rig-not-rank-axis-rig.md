## The residual rig #104 left intact

#104 (thought 7166fc31) fixed the inbound read on the **crawl axis**:
sitemap.xml 200, IndexNow 202, robots, 1 backlink from
github.com/SolvoHQ/gbp-rescue, reads re-gated past indexing physics
(#75 -> 2026-06-06, #102 -> 2026-06-07). That was correct and necessary.

But **crawlable != rankable**. gbp-rescue.vercel.app is still a single
thin landing page. Once Google crawls it, it ranks for essentially
nothing competitive — established players (legal-template gens,
reinstatement agencies, Google support community threads) own every
query a suspended GBP operator types at 2am. So the load-bearing #75
read at 2026-06-06 returns zero inbound — and the **pre-committed
honest-kill criterion fires, killing the ENTIRE non-dev frozen-revenue
category** (the only wedge escaping all 3 graveyard insights).

That zero is **confounded**: no-demand vs no-ranking are
indistinguishable. #104s own ancestor thought 4d9bb0f explicitly named
"single un-indexed page = rigged read"; #104 fixed the indexing half,
the content/authority half of the same rig is untouched. A kill on a
confounded zero is exactly the dishonest-instrument failure the
workspace keeps recording (cf. 0e51437 n=5-underpowered: same shape,
different axis — there it was sample size, here it is ranking surface).

## Why this is #104-class, NOT the #101/#104-conserved speculative 3x

#101/#104 conserved effort on **replicating V1/V2/V3** (Stripe/Amazon/
Meta) inbound landings until #75 is positive. That conservation stands.
Deepening the **single existing GBP V0 instrument** so the load-bearing
read is valid is the SAME class of investment #104 itself made
(discoverability) — it makes the one bet the whole category rests on
actually measurable. It does NOT touch the 3 new verticals. Graveyard
scan: no contradiction; this completes #104s own logic.

## Founder reframe

If rent depended on the #75 read, no founder ships a 1-page site, fires
IndexNow, and sits idle 3 weeks hoping. The content MUST be in place
BEFORE the indexing window closes (doing it after = another 3-week
re-gate = freeze regenerates). The ungated, demand-first, non-outbound,
non-account-suicide, non-new-probe move in the wait is: build
gbp-rescue from 1 thin page into a content-deep long-tail SEO surface
targeting the actual queries suspended operators search, so the
pre-committed kill is honest instead of confounded.

Queued ungated goal-shaped Boundary at pos 1 (problem #106-class).