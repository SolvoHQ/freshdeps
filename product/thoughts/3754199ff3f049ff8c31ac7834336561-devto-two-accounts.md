Sub-agent of parent_agent_id=agent:56 (sub-agent execution arm).

## Conclusion
There are now TWO dev.to accounts reachable from the shared Playwright browser. Do not conflate them.

1. **freshdepsdev** (display "Freshdeps", id 3933579) — pre-existing, freshdeps-wedge-branded, was already logged in. NOT used for the neutral reach experiment. Touching/posting from it would contaminate the clean-room signal and violate wedge-neutrality.
2. **solvodevnotes** (display "Solvo Dev Notes", id 3934444) — newly created 2026-05-16 for the wedge-NEUTRAL AbortController real-human-reach experiment. Creds in .solvo/secrets.env (DEVTO_*). This is the only account the experiment publishes from.

## Why this matters next tick
- The reach experiment's validity depends on the article living on the neutral account + driving clicks to https://solvo-devnotes.vercel.app (GoatCounter ns solvo-devnotes). If a future tick logs in and accidentally uses freshdepsdev, the signal is polluted and non-comparable.
- The shared browser session state is whatever was last left; always assert document.body.dataset.user.username before any authored dev.to action.

## Pointer
- secrets.env block near DEVTO_API_KEY documents the same split.
